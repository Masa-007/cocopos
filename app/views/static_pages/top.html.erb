<main class="flex-grow pt-24">
  <section class="hero relative">
    <div class="container mx-auto max-w-3xl">
      <div class="suggestion-box mt-12 relative">
        <button id="helpButton"
          class="absolute top-4 right-4 z-[9999] text-3xl bg-white shadow-lg rounded-full w-12 h-12 flex items-center justify-center hover:scale-105 transition">
          ❓
        </button>

        <div class="mail-slot">
          <span class="text-white text-lg font-bold tracking-widest">投函口</span>
        </div>
        <div class="floating text-7xl mb-6">cocopos📮</div>

<div class="slideshow-container" 
             data-controller="slideshow" 
             data-slideshow-interval-value="4000">
          <div class="slideshow-slide active" data-slideshow-target="slide">
            <%= image_tag "cocopos.top01.jpg", alt: "心のポストイメージ1" %>
          </div>
          <div class="slideshow-slide hidden" data-slideshow-target="slide">
            <%= image_tag "cocopos.top02.jpg", alt: "心のポストイメージ2" %>
          </div>
          <div class="slideshow-slide hidden" data-slideshow-target="slide">
            <%= image_tag "cocopos.top03.jpg", alt: "心のポストイメージ3" %>
          </div>
        </div>

        <h2 class="text-4xl font-bold mb-4 text-gray-600">
          あなたの心を投函しよう
        </h2>

        <p class="text-lg mb-8 text-gray-600 leading-relaxed">
          「未来への宣言をする・心を整理する・感謝を伝える」ことができます。
        <br class="hidden md:block">
           TODOリスト、アウトプット、感謝ログなど、使い方はあなた次第です。
        </p>

      <% if user_signed_in? %>
        <%= link_to "マイページへ →", mypage_path, class: "btn-primary px-8 py-4 text-xl rounded-lg" %>
      <% else %>
        <%= link_to "ログインしてはじめる →", new_user_session_path, class: "btn-primary px-8 py-4 text-xl rounded-lg" %>
      <% end %>
    </div>
  </section>
</main>