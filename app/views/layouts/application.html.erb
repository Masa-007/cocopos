<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>cocopos - 心のポスト</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Tailwind & Custom CSS -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload", rel: "stylesheet" %>

  <!-- JavaScript -->
  <%= javascript_importmap_tags %>
</head>

<body class="flex flex-col min-h-screen bg-gradient-to-br from-rose-50 to-orange-50 text-gray-800">

  <!-- ヘッダー -->
  <%= render 'shared/header' %>

  <!-- 🔔 フラッシュメッセージ（右上固定・数秒で消える） -->
  <div id="flash-container">
    <% if notice.present? %>
      <div class="flash flash-notice"><%= notice %></div>
    <% end %>
    <% if alert.present? %>
      <div class="flash flash-alert"><%= alert %></div>
    <% end %>
  </div>

  <!-- メインコンテンツ -->
  <main class="flex-grow relative z-0">
    <%= yield %>
  </main>

  <!-- モーダル -->
  <%= render 'shared/modal' %> 

  <!-- フッター -->
  <%= render 'shared/footer' %>

  <!-- フラッシュ自動非表示スクリプト -->
  <script>
    document.addEventListener("turbo:load", () => {
      const flashes = document.querySelectorAll(".flash");
      flashes.forEach(flash => {
        setTimeout(() => {
          flash.classList.add("fade-out");
          setTimeout(() => flash.remove(), 500);
        }, 2000);
      });
    });
  </script>
</body>
</html>
