<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>cocopos - 心のポスト</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <script async src="https://www.googletagmanager.com/gtag/js?id=G-TZCCWQN88R"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-TZCCWQN88R');
  </script>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload", rel: "stylesheet" %>

  <script type="module" src="/assets/application.js" defer></script>

  <script>
    document.addEventListener("turbo:load", () => {
      const flashes = document.querySelectorAll(".flash");
      flashes.forEach((flash) => {
        setTimeout(() => {
          flash.classList.add("fade-out");
          setTimeout(() => flash.remove(), 500);
        }, 2000);
      });
    });
  </script>
</head>

<body class="flex flex-col min-h-screen bg-gradient-to-br from-rose-50 to-orange-50 text-gray-800 <%= @season %> <%= controller_name %>-<%= action_name %>">
  <%= render "shared/header" %>

  <div id="flash-container" class="fixed top-4 right-4 space-y-2 z-50">
    <% if notice.present? %>
      <div class="flash flash-notice bg-green-100 border border-green-300 text-green-700 px-4 py-2 rounded-md shadow-sm transition-all">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="flash flash-alert bg-red-100 border border-red-300 text-red-700 px-4 py-2 rounded-md shadow-sm transition-all">
        <%= alert %>
      </div>
    <% end %>
  </div>

  <main class="flex-grow relative z-0">
    <%= yield %>
  </main>

  <%= render "shared/modal" %> 

  <%= render "shared/footer" %>
</body>
</html>
