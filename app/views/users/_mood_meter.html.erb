<!-- 🌈 気分メーター（カレンダーと同期） -->
<div class="bg-white rounded-3xl border-2 border-purple-200 shadow-md p-5 mb-8">
  <h2 class="text-lg font-bold mb-1 text-gray-800">
    🌈 あなたの気分メーター（心の整理箱）
  </h2>

  <p class="text-sm text-gray-500 mb-2">
    <%= mood_insight %>
  </p>

  <% if mood_chart_data.present? %>
    <canvas
      id="moodChartCanvas"
      class="w-full"
      height="100"
      data-controller="mood-chart"
      data-mood-chart-data-value="<%= mood_chart_data.to_json %>">
    </canvas>
  <% else %>
    <p class="text-sm text-gray-500">
      この月には心の整理箱の投稿がありません。
    </p>
  <% end %>
</div>
