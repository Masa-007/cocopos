<!-- 🌈 気分メーター（カレンダーと同期） -->
<div class="bg-white rounded-3xl shadow-lg p-8 mb-14">
  <h2 class="text-2xl font-bold mb-2 text-gray-800">🌈 あなたの気分メーター（心の整理箱）</h2>
  <p class="text-sm text-gray-500 mb-4"><%= mood_insight %></p>

  <% if mood_chart_data.present? %>
    <canvas
      id="moodChartCanvas"
      class="w-full"
      height="120"
      data-controller="mood-chart"
      data-mood-chart-data-value="<%= mood_chart_data.to_json %>">
    </canvas>
  <% else %>
    <p class="text-gray-500">この月には心の整理箱の投稿がありません。</p>
  <% end %>
</div>
