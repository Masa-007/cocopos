<div class="bg-white rounded-5xl shadow-lg p-8 mb-14">
  <!-- ヘッダー -->
  <div class="flex flex-col items-center text-center text-gray-800">
    <!-- セクション見出し -->
    <p class="text-sm text-gray-500 mb-1">
      感謝箱の積み重ね
    </p>

    <!-- メイン数値 -->
    <div class="flex items-baseline justify-center gap-2 mb-3">
      <span class="text-sm font-medium text-gray-600">
        感謝ポイント
      </span>
      <span class="text-3xl font-bold text-pink-500 leading-none">
        <%= thanks_points %>
      </span>
    </div>

    <!-- アイコン（感情のアクセント） -->
    <span class="text-3xl mb-3">💌</span>

    <!-- 説明文 -->
    <p class="text-sm text-gray-500 leading-relaxed">
      今月に綴った「ありがとう」の数です
    </p>
  </div>

  <!-- 感謝の相手サマリー -->
  <% if thanks_recipients_summary.present? %>
    <div class="mt-8 flex flex-wrap justify-center gap-2">
      <% thanks_recipients_summary.each do |item| %>
        <span class="inline-flex items-center gap-1 rounded-full
                     bg-pink-50 px-3 py-1 text-xs text-pink-700">
          <%= item[:label] %>
          <span class="font-semibold">
            <%= item[:count] %>
          </span>
        </span>
      <% end %>
    </div>
  <% else %>
    <p class="mt-8 text-sm text-gray-500 text-center leading-relaxed">
      まだ感謝が記録されていません。<br>
      身近な人に「ありがとう」を伝えてみましょう！
    </p>
  <% end %>
</div>

