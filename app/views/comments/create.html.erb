<main class="flex-grow pt-24 pb-12 text-center">
  <div class="container mx-auto max-w-2xl px-6">
    <h2 class="text-2xl font-bold text-green-600 mb-6">💬 コメントを投稿しました！</h2>

    <div class="bg-white p-6 rounded-2xl shadow">
      <p class="text-gray-800 whitespace-pre-wrap mb-4"><%= @comment.content %></p>
      <p class="text-sm text-gray-500">
        by <%= @comment.user.display_name || "匿名ユーザー" %> ・
        <%= @comment.created_at.strftime("%Y/%m/%d %H:%M") %>
      </p>
    </div>

    <div class="mt-8">
      <%= link_to "投稿詳細に戻る", post_path(@post),
          class: "inline-block bg-pink-400 text-white px-5 py-2 rounded-xl hover:bg-pink-500 transition" %>
    </div>
  </div>
</main>
