<% flowered = user_signed_in? && flowerable.flowered_by?(current_user) %>

<% url =
     if flowerable.is_a?(Comment)
       post_comment_flower_path(flowerable.post, flowerable)
     else
       post_flower_path(flowerable)
     end %>

<%= form_with url: url,
              method: flowered ? :delete : :post,
              data: { turbo_stream: true },
              class: "flower-form" do %>

  <button type="submit"
          class="flower-btn <%= flowered ? 'interaction-active is-flowered' : 'interaction-inactive is-unflowered' %>">
    <span class="flower-icon"><%= flowered ? "🌿" : "🌱" %></span>
    <span class="flower-count"><%= flowerable.flowers.count %></span>
  </button>

<% end %>
