<% if post.is_public? %>
  <% comment_interacted = user_signed_in? && post.commented_by?(current_user) %>
  <div class="flex items-center gap-3">
    <div id="flower_btn_post_<%= post.id %>">
      <%= render "flowers/button", flowerable: post %>
    </div>

    <% if post.comment_allowed? %>
      <%= link_to post_path(post, anchor: "comment-form", from: from),
                  data: { turbo: false },
                 class: "comment-btn #{comment_interacted ? 'interaction-active' : 'interaction-inactive'}" do %>
        💬 <span><%= post.comments.size %></span>
      <% end %>
    <% end %>
  </div>
<% end %>
